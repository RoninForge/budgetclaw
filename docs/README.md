# docs/

Extended documentation lives here. The top-level [README.md](../README.md) is the entry point for new users; this directory is for deeper guides that would bloat the main readme.

Planned:

- `architecture.md` — how the watcher, parser, evaluator, and enforcer fit together
- `configuration.md` — full TOML schema reference with every field
- `security-model.md` — threat model, what budgetclaw touches vs what it explicitly avoids
- `claude-code-plugin.md` — installing via `claude plugins install budgetclaw`
- `faq.md` — "is this a proxy?", "does this break Anthropic ToS?", "what about Cursor?"

None of these exist yet. Contributions welcome.
