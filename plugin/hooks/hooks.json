{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v budgetclaw >/dev/null 2>&1 && budgetclaw status --short 2>/dev/null || echo '[budgetclaw not installed - run: curl -fsSL roninforge.org/get | sh]'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
